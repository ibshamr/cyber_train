# CyberTrain - Environment Variables
# IMPORTANT: this file must be named exactly ".env" (with the leading dot)
# and must sit next to app.py. Never commit it to git or share it publicly.

# --- Flask session security ---
# Any long random string. Generate one with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=REPLACE_WITH_A_NEW_RANDOM_SECRET

# --- API Keys ---
# Your previous keys were exposed in the app.py source code, so they've been
# removed from this project. Rotate/regenerate each one from its provider
# dashboard, then paste the NEW values below.
OTX_KEY=REPLACE_WITH_NEW_OTX_KEY
VIRUSTOTAL_KEY=REPLACE_WITH_NEW_VIRUSTOTAL_KEY
MXTOOLBOX_KEY=REPLACE_WITH_NEW_MXTOOLBOX_KEY

# --- Admin Credentials ---
ADMIN_USER=admin
ADMIN_PASS=REPLACE_WITH_A_STRONG_ADMIN_PASSWORD

# --- Optional ---
# FLASK_DEBUG=False
# PORT=5000
